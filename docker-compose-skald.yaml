services:
  skald-anonymisation:
    image: ghcr.io/kailash-reddy/skald:latest
    volumes:
      - /tmp/tee_input/data:/app/data
      - /tmp/tee_input/config:/app/config
      - /tmp/tee_output:/app/output
