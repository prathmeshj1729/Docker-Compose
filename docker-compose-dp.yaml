services:
  differential-privacy:
    image: ghcr.io/kailash-reddy/differential-privacy:v1
    volumes:
      - /tmp/DP_input/input_file:/app/data
      - /tmp/DP_input/config:/app/config
      - /tmp/DP_output:/app/output
