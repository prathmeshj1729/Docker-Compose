services:
  image-classifier:
    image: ghcr.io/prathmeshj1729/iudx-differential-privacy-i:latest # Replace with your image name and tag
    volumes:
      - /tmp/DPinput/inputdata:/app/data
      - /tmp/DPinput/config:/app/config
      - /tmp/DPoutput:/app/pipelineOutput
