services:
  differential-privacy:
    image: ghcr.io/kailash-reddy/differential-privacy:v1
    volumes:
      - /tmp/tee_input/input_file:/app/data
      - /tmp/tee_input/config:/app/config
      - /tmp/tee_output:/app/output
